<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Mike Croall Stats</title>
    <style rel="stylesheet" type="text/css">
        html,
        body {
            margin: 0;
            padding: 0;
            height: 100%;
            width: 100%;

            background-color: rgba(51, 51, 51, 1);
            text-align: center;
            font-family: Verdana;

            display: flex;
            justify-content: center;
            align-items: center;
        }

        ul {
            text-align: left;
            border: 5px solid rgba(220, 220, 220, 0.8);
            padding: 20px;
            list-style: none;
            background-color: rgba(51, 51, 51, 0.5);
        }

        ul li {
            color: rgba(220, 220, 220, 0.8);
            text-decoration: none;
            font-size: 4em;
            text-shadow: 5px 5px rgba(51, 51, 51, 0.5);
        }

        .clickCount {
            display: inline;
            color: white;
        }

        @font-face {
            font-family: 'icomoon';
            src: url('fonts/icomoon.eot?69pzyo');
            src: url('fonts/icomoon.eot?69pzyo#iefix') format('embedded-opentype'),
            url('fonts/icomoon.ttf?69pzyo') format('truetype'),
            url('fonts/icomoon.woff?69pzyo') format('woff'),
            url('fonts/icomoon.svg?69pzyo#icomoon') format('svg');
            font-weight: normal;
            font-style: normal;
        }

        [class^="icon-"],
        [class*=" icon-"] {
            /* use !important to prevent issues with browser extensions that change fonts */
            font-family: 'icomoon' !important;
            speak: none;
            font-style: normal;
            font-weight: normal;
            font-variant: normal;
            text-transform: none;
            line-height: 1;

            /* Better Font Rendering =========== */
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        .icon-envelop:before {
            content: "\e945";
        }

        .icon-flickr3:before {
            content: "\eaa5";
        }

        .icon-github:before {
            content: "\eab0";
        }

        .icon-linkedin:before {
            content: "\eac9";
        }
    </style>
</head>

<body>
    <ul>
        <li>
            <span class="icon-envelop"></span> Email
        </li>
        <li>
            <span class="icon-linkedin"></span> LinkedIn
            <div class="clickCount">{{doc.linkedinClicks}} clicks</div>
        </li>
        <li>
            <span class="icon-github"></span> GitHub
            <div class="clickCount">{{doc.githubClicks}} clicks</div>
        </li>
        <li>
            <span class="icon-flickr3"></span> Flickr
            <div class="clickCount">{{doc.flickrClicks}} clicks</div>
        </li>
    </ul>
</body>

</html>
